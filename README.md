# VOSTOK-CLIENT

Canonical build workspace for the VOSTOK Android client.

CI target: source-built JNI (`libsamp.so`) + debug APK. The client baseline is pinned to the Parad1st APK lineage and VOSTOK changes are validated by real GitHub Actions builds.
